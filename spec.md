# 一页问茶 · 春声三鸣 Spec

## 项目目标

构建“一页问茶 · 春声三鸣”网站，围绕杭州市富阳区春建乡茶产业，提供智慧生产展示、茶产品产销助农展示、校企创业双选匹配三类能力。网站需要便于后续替换图片、视频、文字、接口数据和视觉设计稿。

## 用户画像

- 茶农 / 茶园管理者：关注天气、土壤、传感器、病虫害识别、种植建议。
- 茶产业经营者 / 合作方：关注产品展示、品牌故事、采购合作、传播素材。
- 消费者：关注茶叶产品、茶文化、购买咨询入口。
- 大学生创业团队：提交创业计划，寻找茶园资源。
- 茶园主理人 / 乡村创业支持方：发布场地、资源、合作条件，匹配创业项目。

## 页面结构与路由设计

- `/`：全屏首焦体验，以粒子依次聚成茶叶与数字茶山，并由用户进入智慧生产页面。
- `/production`：数智茶鸣｜智慧生产。
- `/market`：香途畅鸣｜产销助农。
- `/matching`：新苗创鸣｜校企双选。
- 顶部导航固定展示三个核心板块，移动端折叠菜单。
- 每个页面保留独立组件与数据入口，避免后续设计导入时互相影响。

### 首焦动画与页面衔接

- 使用用户提供的两份 FBX 模型作为离线构建素材，按表面积采样为 Binary Little-Endian PLY；浏览器不直接加载高面数 FBX。
- 首焦通过独立 WebGL2 iframe 运行生成引擎，采用 Float32 Position/Velocity Ping-pong MRT FBO；每帧不在 CPU 端遍历或回读粒子。
- 首焦采用“散落粒子—茶叶—茶山—数智茶鸣”的分阶段叙事。茶叶约 4 秒完成汇聚后保持等待，只有“显现春山”按钮可推进约 2.8 秒的茶山形变；画布、滚轮及键盘不会跨阶段。
- 引擎会按设备能力使用 1024²、512² 或 256² 粒子档位，并在 WebGL2 浮点渲染不可用时回退为静态茶山图；直达三个业务路由不会下载首焦引擎。
- 首焦使用自定义阻尼相机，可拖拽查看模型的前后与侧面；内置 30 种 GLSL 矢量场、15 套 GSAP 运镜和隔离的 VJ 控制台，支持 ACES、Bloom 与残影后期。
- 支持 `prefers-reduced-motion`，减少动态效果时直接展示静态终态；老年关怀模式跳过首焦。

## 技术栈选择

- 构建工具：Vite。
- 框架：React + TypeScript。
- 样式：Tailwind CSS + 少量全局 CSS 变量。
- 路由：React Router。
- 图标：lucide-react。
- 动效：motion/react，用于轻量进入动效。
- 三维与粒子：生产地图使用 Three.js + React Three Fiber；首焦使用隔离的原生 WebGL2/Three.js GPGPU 子应用，均按路由懒加载。
- 数据：本地 TypeScript mock data。
- 持久化：LocalStorage，仅用于校企双选表单投稿记录。
- 暂不引入复杂后端、鉴权、支付、电商订单、真实文件上传。

## 功能拆分

### 数智茶鸣｜智慧生产

- 科技感首页头图区域，预留茶园视频或背景图 URL。
- 气象数据卡片：天气、温度、湿度、降雨、风力。
- 传感器仪表盘：土壤 pH、土壤湿度、环境温度、光照强度。
- 病虫害识别界面：图片上传占位、预览、mock 识别按钮、结果卡片。
- 识别结果字段：病虫害类型、置信度、简要说明、建议处理方式。
- 农业知识辅助区：种植建议、病虫害防治、施肥建议。
- 后续扩展：真实气象 API、IoT 传感器接口、多模态识别、RAG 知识库。

### 香途畅鸣｜产销助农

- 独立路由，方便后续 Figma 或前端 UI 工具单独替换。
- 茶叶产品展示区，预留产品图、名称、等级、简介、价格或咨询文案。
- 品牌故事区，预留春建乡、茶园、非遗/茶文化文案。
- 茶文化图文区，预留多图内容块。
- 视频展示区，预留茶园航拍、制茶过程、品牌宣传视频 URL。
- 产品卡片区，预留购买 / 咨询 / 合作按钮。
- 后续扩展：商品详情页、咨询表单、外链小程序、轻量 CMS。

### 新苗创鸣｜校企双选

- 大学生创业计划提交表单。
- 茶园资源发布表单。
- 标签/关键词匹配机制。
- 匹配结果卡片，展示推荐茶园或推荐创业项目。
- 表单数据当前保存到 LocalStorage，也提供 mock 默认数据。
- 预留附件、图片、视频上传占位，不做真实上传。
- 后续扩展：账号体系、审核机制、真实数据库、推荐算法、消息沟通。

## 数据结构与 Mock Data 设计

核心类型：

- `WeatherMetric`：名称、值、单位、状态、更新时间。
- `SensorMetric`：名称、值、单位、安全范围、趋势。
- `PestDetectionResult`：图片、类型、置信度、说明、建议。
- `KnowledgeArticle`：标题、分类、摘要、标签。
- `TeaProduct`：名称、图片、简介、规格、标签、咨询链接占位。
- `MediaAsset`：类型、标题、封面、URL、说明。
- `StudentProject`：项目名称、团队介绍、创业方向、所需资源、合作方式、联系方式、标签、附件占位。
- `TeaGardenResource`：茶园名称、位置、可提供资源、支持方向、合作条件、联系方式、媒体占位、标签。
- `MatchResult`：项目、茶园、匹配标签、匹配分数、推荐理由。

mock data 集中放在 `src/data/`，类型集中放在 `src/types/`。所有图片和视频使用资源字段统一维护，后续替换不改组件结构。

## 当前版本实现范围

- 完成 Vite + React + TypeScript + Tailwind 项目骨架。
- 完成三个独立路由和顶部导航。
- 完成三个页面的基础布局和内容占位。
- 完成智慧生产 mock 数据展示、上传识别 mock 交互。
- 完成产销助农独立展示页面骨架。
- 完成校企双选两个表单、LocalStorage 保存、简单标签匹配。
- 完成响应式布局，适配桌面和移动端。
- 不实现真实登录、真实上传、真实支付、真实后端接口。

## 开发步骤

1. 创建 `spec.md` 与 `agent.md`。
2. 初始化 Vite React TypeScript 项目与 Tailwind 配置。
3. 建立目录结构：`components`、`pages`、`data`、`types`、`lib`、`assets`。
4. 实现全局布局、顶部导航和路由。
5. 实现首焦 PLY 粒子资产生成、WebGL2 GPGPU 茶叶到茶山形变及生产地图交接。
6. 实现数智茶鸣页面与 mock 识别交互。
7. 实现香途畅鸣展示页面与资源位。
8. 实现新苗创鸣表单、LocalStorage 和匹配逻辑。
9. 做响应式、动效和视觉统一。
10. 运行类型检查、构建、基础浏览器验证。
11. 启动独立 review 上下文，对照 `spec.md` 做代码审查并修复问题。

## 测试计划

- `npm run build` 验证生产构建。
- TypeScript 类型检查无错误。
- 三个路由可正常访问和互相切换。
- 首焦完整播放、跳过、按钮门控、拖拽观察与进入生产页均可用，进入生产地图时无白屏或重复导航。
- WebGL 失败、减少动态效果和移动端低粒子数量降级正常。
- 移动端、平板、桌面布局无明显重叠和溢出。
- mock 气象、传感器、产品、知识内容正常展示。
- 病虫害上传占位和 mock 结果展示可用。
- 创业计划和茶园资源表单可填写、保存、读取。
- 标签匹配结果可正常生成。
- Review 阶段重点检查组件拆分、类型设计、资源预留、后续可替换性。
